# Universal Statistician

Универсальный статистический помощник: нормализованный, атрибутированный
доступ к официальной статистике из международных источников — за любой
период, с автоматически считаемыми сравнительными таблицами. Каждое число
несёт источник, код датасета, момент получения и ссылку на первоисточник.

Анализ референсных проектов, обоснование стека и полная последовательность
разработки MVP — в [`plan.md`](./plan.md). Ниже — что уже реализовано и как
этим пользоваться.

## Архитектура

Один слой поверх другого, каждый — с собственными тестами:

```
frontend/                React + Vite + TypeScript дашборд поверх REST API (три вкладки)
mcp_server.py / cli.py / api.py   тонкие обёртки трёх интерфейсов поверх tools.py
tools.py                 search_indicator, get_series, compare, list_sources, describe_source, refresh_catalog, catalog_stats
core/compose.py          сравнительные таблицы + вычисляемые колонки (growth/CAGR/index/moving average/...) с lineage (formula/input_series)
core/validation.py       validate_series/validate_table — структурные PASS/WARNING/FAIL находки
core/provenance.py       resolve_provenance() — полная цепочка любой ячейки таблицы вплоть до первичных наблюдений
core/answer.py           AskResult/ChartSpec — модель структурированного ответа
core/ask.py              answer_question() — вся цепочка: план → выбор → ретрив → трансформации → валидация → ответ
core/engine.py           QueryEngine — единая точка входа: провайдеры + Catalog + Cache
core/catalog.py          локальный многоязычный полнотекстовый индекс индикаторов (SQLite FTS5 + метаданные)
core/ingestion.py        discovery → нормализация → каталог (upsert, incremental refresh)
core/query_plan.py       QueryPlan/QuestionInterpretation + build_query_plan() (резолв концептов через каталог)
core/selection.py        select_indicators() — детерминированный, объяснимый выбор одного источника на концепт
planning/                LLMPlanner: RuleBasedPlanner (без LLM) + AnthropicPlanner (forced tool call)
core/cache.py            локальный TTL-кэш поверх get_series() (SQLite)
providers/sdmx_provider.py + registry.py   генерик-провайдер поверх sdmx1, источники — записи в реестре
providers/pxweb_provider.py + pxweb_registry.py   генерик-провайдер поверх pxwebpy (второй, не-SDMX протокол)
chat.py                  CLI-чат поверх Claude API, инструменты и их схемы переиспользуются от mcp_server.py
```

Более крупная инициатива поверх этого MVP — расширение на десятки источников
и естественноязыковой статистик — расписана по фазам в
[`docs/architecture/nl-platform.md`](./docs/architecture/nl-platform.md).

`Provider` — единственный контракт (`get_series`, `describe`), который должен
реализовать источник данных; поиск (`search`) сознательно вынесен из него в
`Catalog`/`QueryEngine`, потому что он межисточниковый по своей природе.
`tools.py` — общая логика для всех трёх интерфейсов: MCP-сервера, CLI и
REST API. Каждый — тонкая обёртка над одними и теми же функциями, а не три
места с одной и той же логикой.

### Два вскрывшихся по ходу дела нюанса

**Запись в реестре — это не "агентство целиком", а конкретный запрашиваемый
датасет.** У World Bank один датафлоу (WDI) с одинаковым порядком измерений
для всех ~1500 индикаторов — одной записи достаточно на весь источник. У
Eurostat и IMF на каждый датафлоу свой DSD со своим набором и порядком
измерений, поэтому запись в реестре относится к одному датафлоу (например,
`ESTAT_NAMA_10_GDP`), а не ко всему агентству. Подробности и источники правды
по форматам ключей — в `providers/registry.py`.

**MCP-сервер выполняет каждый tool-вызов в отдельном worker-потоке.** Это
нашлось только при сквозном тесте через `server.call_tool(...)`, а не через
`tools.py` напрямую: `sqlite3`-соединения `Catalog`/`Cache` по умолчанию
(`check_same_thread=True`) такие вызовы отклоняют. Отдельное соединение на
поток не подходит — для `:memory:`-баз это была бы каждый раз новая пустая
база. Исправлено `check_same_thread=False` + `threading.Lock` на оба класса,
закреплено regression-тестами через `ThreadPoolExecutor`
(`test_catalog.py`, `test_cache.py`) и через реальный `server.call_tool()`
(`test_mcp_server.py`). REST API (`api.py`) подвержен тому же классу риска —
Starlette тоже выполняет синхронные хендлеры в worker-потоке — и прошёл
через `TestClient` без дополнительных правок: фикс уже был общим для
`Catalog`/`Cache`, а не специфичным для MCP.

**Необработанное исключение провайдера ломает CORS, а не просто отдаёт 500.**
Нашлось только при живой проверке дашборда в браузере (не через `TestClient` —
там это выглядело бы просто как 500): когда `get_series`/`compare` падают на
реальном сетевом сбое (например, `api.worldbank.org` заблокирован egress-
политикой), необработанное исключение не долетает до `CORSMiddleware`
нормально — браузер репортит это как "blocked by CORS policy", хотя
реальная причина не в CORS. Исправлено: `_call()` в `api.py` ловит
`Exception` целиком и превращает его в обычный `HTTPException(502, ...)` —
тогда CORS-заголовки проставляются штатно, а сообщение в дашборде честное
("Upstream data source request failed: …"), а не вводящее в заблуждение.
Закреплено `test_provider_failure_is_a_clean_502_with_cors_headers`.

**`PxApi(...)` (pxwebpy) делает сетевой запрос прямо в конструкторе.** В
отличие от `sdmx.Client()`, который ленивый (сеть — только при `.data()`),
`PxApi.__init__` сразу ходит за `/config` и числом таблиц. Если бы
`PXWebProvider` строил его в своём `__init__`, то `default_engine()` —
вызывается при старте КАЖДОГО интерфейса — пытался бы достучаться до SCB ещё
до того, как кто-то вообще запросил что-то из этого источника, и ронял бы
старт всего приложения при недоступности одного-единственного агентства.
Исправлено до того, как это стало багом: `PXWebProvider` строит `PxApi`
лениво при первом `get_series()`, не в конструкторе. Закреплено
`test_construction_does_not_touch_the_network`.

**`chat.py` переиспользует объект `server` из `mcp_server.py`, а не модуль.**
`from universal_statistician.mcp_server import server as _mcp_server` —
`_mcp_server` уже *есть* объект `MCPServer`, а не модуль с атрибутом
`.server`. Первая версия `execute_tool_call` писала
`_mcp_server.server.call_tool(...)` (лишний `.server`) — упало бы на первом
же вызове инструмента. Поймано офлайн-тестом (`test_execute_tool_call_*`),
без необходимости в живом диалоге с Claude.

### Каталог: от статичного seed к discovery-пайплайну (Фаза 1)

`IndicatorMeta`/`IndicatorEntry` расширены необязательными полями (`unit`,
`frequency`, `dataset_id`, `geographic_coverage`, `dimensions`,
`source_organization`, `official_url`, `last_updated`, `keywords`) —
обратно совместимо: старые записи с одним кодом и меткой (как в
`catalog_seed.py`) по-прежнему валидны, просто с пустыми новыми полями.
`Catalog.add()` теперь честный upsert по `(source_id, indicator_id)`, а не
только вставка — повторный запуск ingestion обновляет запись, а не плодит
дубликаты. Провайдер может (необязательно) реализовать
`MetadataDiscoverable.discover_catalog_entries()`
(`providers/base.py`) — тогда `core/ingestion.py` умеет выкачать его каталог
целиком через `ustat catalog refresh [SOURCE_ID]` вместо ручного
перечисления индикаторов. Ни один из зарегистрированных источников пока
этого не реализует (Фазы 2-6) — команда честно репортит
`"does not support metadata discovery"`, а не падает и не делает вид, что
что-то произошло. Подробности архитектуры — в
[`docs/architecture/nl-platform.md`](./docs/architecture/nl-platform.md).

## Покрытие источников

| Источник | Датафлоу/таблица | Статус |
|---|---|---|
| World Bank | WDI (все индикаторы, discovery через `ustat catalog refresh WB_WDI`) | ✅ `WB_WDI` |
| IMF | CPI (Consumer Price Index), discovery через `ustat catalog refresh IMF_DATA_CPI` | ✅ `IMF_DATA_CPI` |
| Eurostat | NAMA_10_GDP (нацсчета/ВВП, текущие цены), discovery через `ustat catalog refresh ESTAT_NAMA_10_GDP` | ✅ `ESTAT_NAMA_10_GDP` |
| Statistics Sweden (SCB) | TAB6471 (PX-Web, не SDMX), discovery через `ustat catalog refresh SCB_TAB6471` | ✅ `SCB_TAB6471` |
| US Census Bureau | ACS 1-Year Estimates (REST/JSON, третий, не-SDMX/не-PXWeb протокол), discovery через `ustat catalog refresh US_CENSUS_ACS1` | ✅ `US_CENSUS_ACS1` |
| OECD | — | не добавлен: типы эндпоинтов (`datastructure`/`dataflow`/`codelist`) поддерживаются, но нет ни одного проверенного примера запроса к ним, только к `data` без ключа + единственный проверенный пример с ключом (`OECD_JSON`) требует небезопасного SSL-даунгрейда — см. `registry.py`/`docs/architecture/nl-platform.md` |
| Statistics Norway (SSB) | — | не добавлен: реальный, известный библиотеке `pxweb` шорткат (`"ssb"`), но нет проверенного table_id — угадывать не стал |
| Росстат / ЕМИСС | — | не добавлен: см. ниже |

Каталог индикаторов пока содержит только четыре уже проверенных в
`get_series()` записи — по одной на источник (`providers/catalog_seed.py`).
Полное покрытие каждого датафлоу/таблицы требует живого запроса к
codelist/conceptscheme (SDMX) или table-variables (PX-Web) источника,
недоступного в песочнице разработки (см. «Тесты» ниже).

### Почему нет Росстат/ЕМИСС

`fedstat.ru` сам по себе заблокирован egress-политикой этой песочницы (не
только этот домен — проверено, что вообще любой внешний хост, кроме
pypi/npm/github/anthropic, недоступен), так что живая проверка исключена
полностью, даже получить одну HTML-страницу не вышло. Хуже того: у fedstat.ru
нет документированного публичного API — доступ к данным идёт через
скрейпинг HTML/JS с внутренней страницы индикатора (единственная найденная
референсная реализация, R-пакет `fedstatAPIr`, сама описывает это как
"эмпирически определённый 12-й `<script>`-тег" — то есть недокументированный
и по своей природе хрупкий механизм). Для SDMX-источников и SCB (см. выше)
у меня была возможность сверяться с проверенным тест-сьютом реальной
библиотеки — здесь такого источника истины нет и быть не может без живого
доступа. Ставить в реестр угаданную логику скрейпинга для страницы, которую
я ни разу не видел, — именно та ошибка, которую этот проект последовательно
избегает (тот же принцип, что и решение не добавлять OECD). Возвращаемся к
этому источнику, когда либо появится сетевой доступ, либо Росстат
опубликует документированный API.

## Установка

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
```

## Запуск

**MCP-сервер** (stdio-транспорт, добавить в конфиг MCP-хоста — Claude Desktop/Code и т.п.):

```bash
universal-statistician-mcp
```

Инструменты: `search_indicator`, `get_series`, `compare`, `list_sources`,
`describe_source` (сигнатуры и докстринги — в `mcp_server.py`, докстринг
становится описанием инструмента для LLM-хоста).

**CLI** — та же логика, для разработки/smoke-тестов без MCP-клиента, вывод JSON:

```bash
ustat sources
ustat source WB_WDI
ustat search population
ustat series WB_WDI SP_POP_TOTL AFG --start 2015 --end 2020
ustat compare WB_WDI --indicator SP_POP_TOTL --ref-area AFG --ref-area USA --growth --rank
ustat compare WB_WDI --indicator-id SP_POP_TOTL --indicator-id NY.GDP.MKTP.CD --for-area AFG
ustat catalog stats             # индикаторов в каталоге, по источнику
ustat catalog refresh           # discovery-ingestion для всех источников, что его поддерживают
ustat catalog refresh WB_WDI    # то же самое для одного источника
ustat plan "population"         # структурный query plan (без LLM: вся фраза = один поисковый концепт)
ustat plan "GDP growth" --llm   # то же самое через Claude (AnthropicPlanner, нужен ANTHROPIC_API_KEY)
ustat ask "population"          # полный пайплайн: план → выбор → ретрив → трансформации → валидация → ответ
ustat ask "GDP growth" --llm    # то же самое, интерпретация через Claude
```

Некорректный запрос (неизвестный источник, неполный `compare`) печатает
понятное сообщение в stderr и завершает процесс кодом 1, а не сырым traceback.

Каталог по умолчанию хранится в файле `~/.universal_statistician/catalog.db`
(SQLite) и переживает перезапуск процесса — `ustat catalog refresh` больше
не нужно перезапускать после каждого рестарта приложения. Путь можно
переопределить переменной окружения `USTAT_CATALOG_DB_PATH`; значение
`:memory:` возвращает старое поведение (не сохраняется между запусками) —
именно это использует весь тест-сьют (`tests/conftest.py`), чтобы тесты не
трогали файл на диске разработчика.

**REST API** — та же логика по HTTP, с автогенерируемой OpenAPI-документацией:

```bash
uvicorn universal_statistician.api:app --reload
# http://127.0.0.1:8000/docs — интерактивная документация
```

| Метод и путь | Соответствует |
|---|---|
| `GET /sources` | `tools.list_sources` |
| `GET /sources/{source_id}` | `tools.describe_source` (404, если источник неизвестен) |
| `GET /search?q=...&limit=` | `tools.search_indicator` |
| `GET /series?source_id=&indicator_id=&ref_area=&start_period=&end_period=` | `tools.get_series` |
| `POST /compare` (JSON-тело = параметры `tools.compare`) | `tools.compare` (400 при некорректной форме запроса) |
| `POST /plan` (`{"question": ..., "use_llm": false}`) | `tools.build_plan` — inspectable-план без ретрива |
| `POST /ask` (`{"question": ..., "use_llm": false}`) | `tools.ask` — полный пайплайн, structured answer |

`use_llm: true` включает `AnthropicPlanner` через `ANTHROPIC_API_KEY` **сервера** (никогда не из тела запроса) — без ключа отдаёт чистый `400`, не падение.

**Веб-дашборд** (React + Vite + TypeScript, требует запущенный REST API):

```bash
cd frontend
npm install
npm run dev            # http://127.0.0.1:5173, ждёт API на http://127.0.0.1:8000
# или сборка статики: npm run build && npm run preview
```

Четыре вкладки — «Спросить» (первая/по умолчанию, естественноязыковой
вопрос → `POST /ask`: ответ, график, таблица, источники, допущения,
предупреждения, бейдж валидации с разворачиваемыми находками, и два
разворачиваемых debug-блока — полный query plan и provenance, реальный
JSON, не выжимка; примеры честно разделены на «работают без Claude» и
«нужен Claude», чекбокс «Использовать Claude» передаёт только булев флаг
`use_llm`, ключ никогда не вводится в браузере — только из окружения
сервера), «Поиск» (работает без сети, поверх локального каталога), «Ряд» и
«Сравнение» (форма → таблица + линейный график через `recharts`, с общей
`_call`-обработкой ошибок так же, как в `api.py`: сетевой/HTTP-сбой
показывается понятным баннером, а не белым экраном). CORS настроен на
`api.py` под любой локальный origin (dev-сервер Vite или собранная статика).

**Чат** — интерактивный диалог с Claude, использующий те же инструменты, что
и MCP-сервер (схемы и выполнение переиспользуются напрямую от
`mcp_server.server`, не описаны и не продублированы заново):

```bash
export ANTHROPIC_API_KEY=...   # https://console.anthropic.com/
ustat chat                     # модель по умолчанию — claude-sonnet-5, флаг --model для другой
```

Без `ANTHROPIC_API_KEY` команда сразу и понятно завершается кодом 1, а не
падает при первом сетевом вызове. В этой песочнице ключа нет — живой диалог
не проверялся ни разу, только структура: конвертация схем инструментов
MCP → формат Claude API, выполнение вызовов через настоящий
`mcp_server.server.call_tool(...)`, и цикл `ChatSession` — на объектах
`anthropic.types.Message`/`TextBlock`/`ToolUseBlock`, собранных вручную
(реальные типы SDK, не выдуманная форма ответа), а не на живом клиенте.
Прогнать первый настоящий диалог и проверить его — на машине с ключом.

**Python API** напрямую через ядро:

```python
from universal_statistician.core.engine import default_engine
from universal_statistician.core.compose import compare_across_countries, with_growth, with_rank

engine = default_engine()

result = engine.get_series("WB_WDI", "SP_POP_TOTL", "AFG", start_period="2011", end_period="2020")
print(result.as_dict())

engine.search_indicator("produit")  # -> ESTAT_NAMA_10_GDP / B1GQ, по французской метке

table = compare_across_countries(engine, "WB_WDI", "SP_POP_TOTL", ["AFG", "USA", "LUX"], start_period="2015")
print(with_rank(with_growth(table)).as_dict())
```

## Тесты

```bash
pytest                 # офлайн-тесты (по умолчанию сеть не используется)
pytest -m network      # + живые запросы к World Bank / IMF / Eurostat / SCB (нужен доступ в интернет)
```

`WorldBankProvider`'s discovery (`test_worldbank_discovery.py`,
`test_worldbank_provider.py`) следует тому же принципу: парсинг проверен на
payload, собранном по опубликованной документации World Bank v2 API,
пагинация — на фейковой сессии, а единственное, что реально требует сети
(сам ответ живого эндпоинта), помечено `network` и один раз прогнано вживую
из песочницы ровно настолько, чтобы подтвердить URL и корректную обработку
сетевого сбоя (см. `docs/architecture/nl-platform.md`).

`IMFProvider`'s discovery (`test_imf_provider.py`) идёт ещё дальше того же
принципа: не просто payload, а полноценный `StructureMessage`, собранный из
настоящих классов `sdmx.model.v21`
(`DataStructureDefinition`/`Dimension`/`Codelist`/`Item`) — включая тест на
несовпадение числа измерений (явная ошибка вместо тихого неверного
маппинга). Провайдерная логика тестируется на реальных объектах `sdmx.model.DataSet`,
собранных в памяти (`tests/conftest.py`), а не на выдуманной структуре.
Значения ключей (`_build_key`) сверяются с примерами из собственного
интеграционного тест-сьюта `sdmx1` (`sdmx/tests/test_sources.py`) — с
запросами, которые мейнтейнеры библиотеки гоняют против живых API.
`PXWebProvider` тестируется тем же принципом: строки для парсинга получены
через реальную функцию `pxweb._internal.functions.unpack_table_data`,
скормленную сконструированному JSON-stat2 (`tests/test_pxweb_provider.py`),
а не через выдуманную форму ответа. Живые тесты (`-m network`) бьют в
`api.worldbank.org`, `data.imf.org`, `ec.europa.eu`, `statistikdatabasen.scb.se`
напрямую; в песочнице разработки они заблокированы сетевой политикой (egress
403 через прокси — и не только эти хосты, см. раздел про Росстат/ЕМИСС выше)
— это ограничение окружения, а не самих провайдеров, и такие тесты стоит
прогнать перед реальным использованием на машине с доступом в интернет.

Все три бэкенд-интерфейса тестируются через свои собственные протоколы вызова
(`server.call_tool(...)`, `typer.testing.CliRunner`, `fastapi.testclient.TestClient`),
а не только через `tools.py` напрямую — именно так нашёлся баг с потоками,
описанный выше.

Дашборд отдельным JS-тест-фреймворком не покрыт (соответствует объёму
проверки личного инструмента — сам REST-контракт, который он вызывает, уже
покрыт `test_api.py`). Проверен вручную живым браузером (Chromium,
Playwright) через все три вкладки: «Поиск» — полностью вживую (реальные
данные из локального каталога), «Ряд»/«Сравнение» — что форма отправляется
и HTTP/сетевая ошибка показывается понятным баннером, а не белым экраном.
Именно эта проверка (не `TestClient`) нашла баг с CORS/502, описанный выше.

`chat.py` тестируется на той же грани честности: `get_tool_schemas`/
`execute_tool_call` — реальными вызовами в `mcp_server.server` (те же
сетенезависимые инструменты, что уже проверены в `test_mcp_server.py`), а
цикл `ChatSession` — на фейковом Anthropic-клиенте, собранном из настоящих
`anthropic.types.Message`/`TextBlock`/`ToolUseBlock` (реальные типы SDK, не
угаданная форма). Без `ANTHROPIC_API_KEY` в этой песочнице живой диалог с
Claude не проверялся ни разу — честно зафиксировано, а не молчаливо
предположено.

## Оценка по внешнему бенчмарку

[`docs/benchmarks/uosa-bench-v1-assessment.md`](./docs/benchmarks/uosa-bench-v1-assessment.md) —
честная оценка продукта по присланному пользователем `UOSA-Bench v1.0`
(1000-балльный бенчмарк для статистических AI-ассистентов, 12 блоков, 7
gate-критериев). Дословно исполнить бенчмарк в этой среде нельзя (сеть
заблокирована, нет ключа Anthropic API, каталог — 4 индикатора вместо 20
стран × 15 тематик) — вместо выдуманных баллов документ даёт
классификацию по блокам/gate с точными ссылками на код и то, что реально
прогнано офлайн. Итог коротко: три самых опасных gate (фабрикация данных,
ложная атрибуция, скрытые оценки) проходятся **по конструкции кода**, но
целый блок G (revisions/vintage/конфликты источников), Confidence Status,
поле `unit`/`formula` в provenance и 15 из 20 операций Golden Derivation
Suite (CAGR, currency conversion, index rebasing и др.) — не реализованы.
Формальный вердикт бенчмарка — FAIL по правилу "любой gate fail = FAIL",
из-за GATE-05 (методологические разрывы, механизма обнаружения нет).
Документ заканчивается приоритизированным списком того, что дешевле всего
исправить в рамках уже существующей архитектуры.

## Что дальше

Границы MVP, обоснование решений и полная последовательность разработки — в
[`plan.md`](./plan.md). MVP, REST API, веб-дашборд, второй (не-SDMX)
источник и CLI-чат готовы. Росстат/ЕМИСС осознанно отложены (см. выше).

Более крупная инициатива — расширение на десятки источников +
естественноязыковой статистик поверх текущего ядра, все 13 фаз готовы,
статус по каждой — в
[`docs/architecture/nl-platform.md`](./docs/architecture/nl-platform.md).
Фазы 1-4 готовы: архитектура каталога, discovery-покрытие World Bank (через
его v2 REST API), IMF (настоящий SDMX structure-запрос к `DSD_CPI`) и
Eurostat (тот же путь, что использует сам `sdmx1` в своём тест-сьюте, с
поправкой на реальную особенность ESTAT — DSD в ответе `dataflow` приходит
внешней ссылкой-заглушкой, нужен второй запрос на резолв). Логика
"DSD → каталоговые записи" для IMF и Eurostat теперь общая
(`providers/sdmx_discovery.py`). Фаза 5 (OECD) — расследована повторно и осознанно НЕ интегрирована, но по
более узкой и точной причине, чем в первом черновике (собственный
регрессионный тест поймал избыточно широкое утверждение до того, как оно
осело в документации): типы эндпоинтов `datastructure`/`dataflow`/
`codelist` у OECD ЗАЯВЛЕНЫ поддерживаемыми (не `structure: false` для всего
подряд) — но ни для одного из них нет проверенного рабочего примера в
тест-сьюте `sdmx1`, только для `data` без фильтрующего ключа (весь датафлоу
целиком). Единственный проверенный пример с ключом (`OECD_JSON`) требует
небезопасного даунгрейда SSL/TLS для подключения (предупреждение об этом —
в докстринге самой библиотеки). Архитектурно ничего не меняется — как
только появится проверенный пример structure-запроса по текущему
`sdmx.oecd.org` API, добавление пройдёт тем же путём, что IMF/Eurostat.
Фаза 6 готова: PX-Web discovery обобщён (не по-агентски, как SDMX, а сразу
на уровне `PXWebProvider` — тот же `get_table_variables()`, что уже
использует `get_series()`, просто читает ещё одно поле), плюс добавлен
`CensusProvider` — третий, принципиально другой протокол (обычный REST/JSON
US Census Bureau, не SDMX и не PX-Web/JSON-stat), с честной оговоркой:
подтверждено только документацией, не тест-сьютом зависимости (как у
SDMX/PX-Web), поэтому без seed-записи в каталоге до подтверждения вживую.
Фаза 7 готова: `core/query_plan.py` (`QueryPlan`/`QuestionInterpretation`)
и `planning/` (`RuleBasedPlanner` без LLM + `AnthropicPlanner` через
forced tool call — модель физически не может подставить в план код
индикатора, для этого в схеме плана просто нет поля; коды резолвятся
только через `QueryEngine.search_indicator()`, реальный каталог).
`ustat plan "<question>"` — inspectable-план без ретрива, по требованию
секции 10. Фаза 8 готова: `core/selection.py::select_indicators()` —
детерминированный, объяснимый выбор ровно одного источника на концепт
(качество совпадения имени + географическое покрытие + частота, каждый
критерий с явной причиной в `assumptions`, "неизвестно" не штрафуется
наравне с "точно не покрывает"). "Никогда не смешивать несовместимые ряды
молча" — не отдельная проверка, а следствие конструкции: один источник на
концепт всегда, и если разные концепты одного плана разошлись по разным
источникам — это явно попадает в `assumptions`. Фаза 9 готова: `core/compose.py` пополнился lineage (`formula`/
`input_series` на каждой производной колонке — главный пункт из оценки по
бенчмарку закрыт) и десятком новых операций (`with_cagr`,
`with_cumulative_growth`, `with_index`, `with_moving_average`,
`with_difference`, `with_share`, `with_per_capita`, `with_sum`/
`with_average`/`with_weighted_average`, `with_absolute_change`,
`with_pp_change`). `core/validation.py` — структурные PASS/WARNING/FAIL
находки (`validate_series`/`validate_table`): цитирование обязательно
(FAIL без источника), unit/frequency-согласованность (WARNING только
когда ОБА значения известны и расходятся — неизвестное не считается
противоречием), покрытие запрошенных регионов/периода, пробелы в годовом
покрытии, целостность lineage производных колонок.

Фаза 10 готова: `core/provenance.py::resolve_provenance()` строит полную
цепочку от любой ячейки таблицы (базовой или производной, на любой
глубине вычислений) до первичных наблюдений — ровно в формате примера из
секции 17 ("Calculated by Universal Statistician" + formula + inputs с
источником/датасетом/временем получения). Честно зафиксированный предел:
для операций, зависящих от нескольких периодов одного и того же входа
(`with_growth`, `with_cagr`, moving average), резолвер не гадает, какой
именно период сыграл роль — прикладывает провенанс по каждому периоду с
данными и явно объясняет почему, вместо правдоподобно выглядящей, но
потенциально неверной точной ссылки.

Фаза 11 готова: `core/answer.py` (`AskResult`/`ChartSpec` — модель ответа
из секций 18/20/21) и `core/ask.py::answer_question()` — вся цепочка из
целевой архитектуры (секция 9), теперь по-настоящему собранная: план →
выбор → **ретрив** (по каждой выбранной паре индикатор×география через
`QueryEngine.get_series()`, отказ по одной паре — предупреждение, не
падение всей таблицы) → **трансформации** (`plan.transformations`
диспетчеризуются по имени на функции `compose.py`, не требующие
дополнительных аргументов — `growth`/`cagr`/`rank`/... — честно
ограниченный охват для первой версии, отмечено предупреждением, не
проигнорировано молча) → **валидация** → **ответ** (текст собирается
шаблоном только из провалидированной таблицы — никогда LLM не
переписывает числа, секция 18) → provenance/chart/sources.

Выставлено на всех трёх интерфейсах: `POST /ask`/`POST /plan` (api.py,
`use_llm` только как флаг, ключ — из окружения сервера, никогда из тела
запроса), `ustat ask`/`ustat plan --llm` (cli.py), и MCP-инструмент `ask`
(сознательно только на `RuleBasedPlanner` — у MCP-хоста уже есть своё
понимание естественного языка, второй внутренний LLM-вызов был бы
избыточен). Живые прогоны подтвердили все три: `ustat ask population`,
`POST /ask`/`POST /plan` по-настоящему через HTTP (включая чистый `400` на
`use_llm=true` без ключа).

Фаза 12 готова: вкладка «Спросить» в дашборде (см. выше) — типизированный
`ask()`/`buildPlan()` в `api.ts`, зеркальные TypeScript-интерфейсы в
`types.ts`, весь результат из секции 24 (ответ, график, таблица,
источники, допущения, предупреждения, бейдж валидации, разворачиваемые
query plan/provenance). Проверено вживую через Playwright/Chromium (не
только `tsc --noEmit`) — реальный ответ от реального бэкенда, честно
показанное ограничение "география не определена" со стилизованным
предупреждением, разворачивающийся debug-блок с настоящим JSON, и
регрессионная проверка — переключение на «Поиск» после этого работает без
изменений.

Отдельно, из оценки по бенчмарку: `formula`/`input_series` в provenance
derived-таблиц и явная `status`-таксономия (Official/Derived/Composite/
User-defined/Estimated) — обе идеи уже закрыты Фазами 9-10 в духе, близком
к первоначальному запросу бенчмарка.

Фаза 13 готова — последняя из 13, все фазы плана выполнены. Три части:
бенчмарк-тесты, structured logging, performance/hardening-проверки.

Шесть бенчмарк-тестов (`tests/test_benchmarks.py`) — дословно примеры
вопросов из секции 25 задания, каждый прогнан через настоящий
`answer_question()` (не мок), со `ScriptedPlanner` вместо живого LLM (в
песочнице нет `ANTHROPIC_API_KEY` — честно зафиксировано, как и в Фазах
7/11/12), но против реального синтетического многострано-многоиндикаторного
каталога. Проверяется не совпадение с "золотыми" числами (их и не может
быть для не-live данных), а структура ответа: правильные колонки/страны,
`derived=True` + `formula` + `input_series` на производных колонках,
provenance резолвится, статус валидации.

Структурное логирование (`logging`, без новой зависимости) — `catalog_search`,
`cache_hit`/`cache_miss`/`provider_request_completed` (с `elapsed_ms`) в
`core/engine.py`, `ask.question_received`/`ask.plan_built`/
`ask.transformations_applied`/`ask.completed` в `core/ask.py`. Проверено не
просто "есть в коде", а `caplog`-тестами (`tests/test_observability.py`) —
конкретные поля конкретных записей.

Performance/hardening (секция 27): два регрессионных теста в
`tests/test_engine.py` доказывают конструкцией, а не комментарием, что
`default_engine()` (стартует при любом запуске CLI/API/MCP) не делает ни
одного сетевого запроса, и что обычный `get_series()`/`search_indicator()`
никогда не запускает discovery/ingestion как побочный эффект — это должно
оставаться явным, admin-triggered действием (`refresh_catalog()`).

Живой прогон: `ustat ask "population"` и `asyncio.run(server.list_tools())`
(6 инструментов: `ask`, `compare`, `describe_source`, `get_series`,
`list_sources`, `search_indicator`) подтвердили, что новое логирование не
сломало ни один существующий интерфейс. **227 офлайн-тестов** (было 219
после добавления логирования, 215 до начала Фазы 13).

Все 13 фаз исходного плана расширения (раздел 28 задания) закрыты. Что
реально не сделано — честно перечислено в разделе "What's genuinely not
done" в [`docs/architecture/nl-platform.md`](./docs/architecture/nl-platform.md):
частичный охват авто-диспетчеризации трансформаций в `/ask`, честный предел
provenance для многопериодных формул, отсутствие живого LLM-прогона в этой
песочнице, discovery для Фаз 2-6 не подтверждён против живых API (кроме
разовых URL-проверок).

## Этап "реальная проверка" (Фазы A–K)

Архитектура из 13 фаз выше признана достаточной. Следующий этап — не новая
архитектура, а превращение технически полной системы в ассистента, чьи
ответы реально проверены против настоящих официальных данных: живая
проверка источников, промышленный каталог, качество поиска, точные
вычисления и provenance, измеримая end-to-end точность. Статус — в
[`docs/architecture/provider-verification-matrix.md`](./docs/architecture/provider-verification-matrix.md).

**Фаза A готова (с честной оговоркой)**: `pytest -m network` и живые
smoke-тесты, которые эта фаза требует, физически не выполнить из этой
песочницы — egress-политика блокирует `api.worldbank.org` (и любой другой
внешний хост, кроме pypi/npm/github/anthropic), подтверждено напрямую через
`curl` и статус прокси (`connect_rejected`, `403`), не предположено. Вместо
притворной "живой" проверки эта фаза сделала то, что реально можно
проверить статическим анализом кода — совместимость идентификаторов между
discovery (каталогизация) и retrieval (`get_series()`) для каждого
источника — и нашла один настоящий, ранее незамеченный баг: World Bank's
`worldbank_discovery.py` брал код индикатора прямо из REST API в
точечной нотации (`"NY.GDP.PCAP.CD"`), а SDMX-ключ для retrieval требует
подчёркивания (`"NY_GDP_PCAP_CD"`, проверено тест-сьютом `sdmx1`) — точка
внутри значения ключа ломает сам SDMX-ключ на лишние сегменты. Любой
индикатор, найденный через `refresh_catalog("WB_WDI")`, был бы после этого
нечитаем через `get_series()`. Исправлено в `parse_indicator()`
(нормализация точка→подчёркивание, оригинальный код сохранён в `keywords`
для поиска), с регрессионным тестом. IMF/Eurostat/PX-Web/Census такой
проблемы не имеют — их discovery и retrieval берут коды из одного и того
же словаря. Полная матрица (discovery/retrieval/geography/period/unit/
provenance/live per источник, честно помеченная NOT VERIFIED в колонке
"Live verified" для всех источников до реального сетевого прогона) — в
файле выше. **228 офлайн-тестов** (было 227).

**Фаза B готова**: производственный workflow наполнения каталога +
персистентность. Механика была уже собрана в Фазах 2-6
(`ustat catalog refresh [SOURCE]` уже вызывал discovery для всех пяти
источников, включая SCB/Census — `refresh_all()` фильтрует провайдеров по
структурному протоколу `MetadataDiscoverable`, а не по списку, так что
PXWebProvider/CensusProvider уже были в нём "бесплатно"); чего не было —
самой персистентности и содержательной статистики. Оба закрыты:

- `core/engine.py::_open_catalog()` — каталог теперь по умолчанию открывает
  настоящий файл SQLite (`~/.universal_statistician/catalog.db`,
  переопределяется `USTAT_CATALOG_DB_PATH`, `:memory:` — старое поведение),
  а не эфемерную `:memory:` базу, пересоздаваемую с нуля при каждом
  перезапуске. Каталог сеется из `catalog_seed.py` только при первом
  открытии ещё пустого файла — повторные перезапуски НЕ пересеивают поверх
  уже найденных через discovery данных (это было бы тихой потерей более
  богатых метаданных, найденных `ustat catalog refresh`, каждый раз при
  рестарте — отдельно проверено регрессионным тестом).
- `Catalog.summary()` (используется `ustat catalog stats`) — вместо
  плоского "индикаторов на источник" теперь: число источников, число
  датасетов, число индикаторов, разбивка по источникам, время последнего
  обновления (общее и по каждому источнику). Старый `Catalog.stats()`
  оставлен как есть — используется внутри `summary()` и другим кодом,
  обратная совместимость не нарушена.
- Идемпотентность `ingest_source()`/`Catalog.add()` уже была реализована
  корректно (DELETE+INSERT для FTS, `INSERT OR REPLACE` для метаданных по
  первичному ключу `(source_id, indicator_id)`) — Фаза B добавила
  регрессионный тест, проверяющий это на уровне реальных строк в БД
  (`test_refreshing_the_same_entries_twice_does_not_duplicate_rows`), а не
  только по семантике `IngestionReport.added/updated/unchanged`, как было
  раньше.

Честная оговорка на момент написания (продолжение той же из Фазы A):
реально выполнить `ustat catalog refresh` против живых официальных API в
этой песочнице было нельзя — сеть заблокирована политикой окружения.
Механизм собран и протестирован офлайн (fake-провайдеры, реальные форматы
ответов), готов пропустить настоящий трафик. **234 офлайн-теста** (было
228).

**Обновление (после переключения окружения на живой доступ, тот же
раунд, что и живая проверка выше)**: `ustat catalog refresh` реально
выполнен против всех пяти источников. Итог — **38 789 настоящих
индикаторов**: World Bank WDI 1498, Eurostat NAMA_10_GDP 643 (не 2 —
полный na_item codelist дataflow'а, не только B1GQ/B1G), IMF CPI 15,
Census ACS1 36 632, SCB TAB6471 1. По пути найден и исправлен ещё один
настоящий, живой баг производительности: `Catalog.get()` фильтровал
FTS5-таблицу `indicators` по UNINDEXED-колонкам — это форсирует полный
скан таблицы на каждый вызов, а `core/ingestion.py`'s change-detection
цикл вызывает `.get()` один раз на КАЖДУЮ найденную запись — то есть
O(n²) на количество записей. Для одного источника с 36 632 записями (US
Census ACS1) это превращало `ustat catalog refresh` в операцию на многие
минуты. Исправлено: `catalog_meta` (обычная таблица с настоящим PRIMARY
KEY-индексом) получила колонки `name`/`description`, `.get()` теперь
читает только оттуда, ни разу не трогая FTS5-таблицу; `Catalog.add()`
переписан на пакетные `executemany()` вместо цикла с отдельными
`execute()`; `ingest_source()` теперь передаёт в `catalog.add()` только
РЕАЛЬНО новые/изменённые записи — для неизменных запись в FTS вообще не
трогается. Результат на синтетическом бенчмарке 36 632 записей: первое
наполнение 0.48с, идемпотентный повтор 0.29с, повтор с одним реальным
изменением 0.29с (было — многие минуты). На реальном каталоге: первый
полный `refresh` — ~2 минуты (упирается в сеть, не в БД), идемпотентный
повторный прогон всех 38 789 записей — **10.7 секунды**, без единого
дубликата. **285 офлайн-тестов** (было 282).

Фаза C (качество поиска по каталогу) была отложена по решению пользователя
— бенчмарк по 50+ концептам не имел смысла против каталога из 4
seed-записей. Условие снято: каталог теперь реально наполнен (38 789
записей, выше) — Фаза C выполнена, см. отдельный раздел ниже.

**Фаза D готова**: `QueryPlan.transformations`/`QuestionInterpretation.
transformations` — теперь не голые строки (`"growth"`), а структурные
`TransformationSpec` (`core/query_plan.py`): `operation` плюс, для операций
над двумя рядами, натурально-языковые ссылки на концепты
(`numerator_concept`/`denominator_concept` для `share`/`per_capita`,
`left_concept`/`right_concept` для `difference`, `input_concept` +
`base_period`/`base_value` для `index`, `inputs`/`weights` — коды
географий, не концепты — для `weighted_average`). Планировщик описывает,
ЧТО посчитать, никогда не подставляет число — код индикатора внутри
трансформации в принципе невозможен: в схеме просто нет для него поля,
только натурально-языковая фраза, ровно тот же структурный принцип
анти-галлюцинации, что уже применён к `QuestionInterpretation.concepts`
(Фаза 7).

Ключевое расширение — `build_query_plan()` теперь резолвит через каталог
не только `interpretation.concepts`, но и любой концепт, упомянутый ТОЛЬКО
внутри трансформации (`TransformationSpec.concepts_referenced()`):
планировщику не нужно дублировать "non-oil GDP" и в `concepts`, и в
`numerator_concept` — он называет концепт один раз, там, где он
естественно появляется, а `QueryPlan.concepts` (в отличие от
`interpretation.concepts`) — это объединение обоих множеств, поэтому
`core/selection.py::select_indicators()` (которая проходит по
`plan.concepts`) реально выбирает индикатор и для концептов,
упомянутых только в трансформации.

Диспетчеризация (`core/ask.py`) резолвит каждый концепт трансформации в
конкретную колонку таблицы через `CandidateIndicator.concept` →
`ComparisonColumn.indicator_id`/`.ref_area` (те же поля, что уже
использует provenance с Фазы 10) — никогда по угаданному ключу. Для
`share`/`per_capita`/`difference`/`index` результат считается отдельно на
каждую запрошенную географию (`AZE__non_oil_share`, и т.д.). Три новые
точные (не "для каждой прочей базовой колонки", как старые
`with_share`/`with_per_capita`/`with_index`) функции в `core/compose.py`:
`with_share_pair`, `with_per_capita_pair`, `with_index_column` — работают
ровно с названными колонками, не задевая остальные (важно: если в той же
таблице есть, например, колонка population для отдельной `per_capita`-
трансформации, `index` не должен её тоже переиндексировать). Нерезолвленный
концепт/недостающие данные — предупреждение, не падение всего ответа;
базовые индикаторы всё равно возвращаются.

Обратная совместимость: `TransformationSpec.from_dict()` принимает и
голую строку (`"growth"` → `TransformationSpec(operation="growth")`), и
новый структурный объект — старые вызовы `/plan`/`/ask` с
`{"transformations": ["growth"]}` продолjают работать без изменений.

Схема инструмента `AnthropicPlanner` (`propose_query_plan`) и системный
промпт обновлены под новую форму `transformations` (JSON Schema объектов
с `operation` + опциональными полями конкретной операции), с явной
инструкцией не подставлять коды и не дублировать концепт в `concepts`.
Фронтенд (`types.ts`) обновлён под новый тип `TransformationSpec[]` —
debug-блок `AskTab.tsx` просто печатает JSON, менять компонент не
потребовалось; `tsc -b && vite build` проходят чисто.

**252 офлайн-теста** (было 234).

**Фаза E готова**: точная lineage для производных значений. Раньше
`ComparisonColumn.input_series` называл только КОЛОНКИ, из которых
посчитано производное значение, — общее на всю колонку, а не на
конкретный период; для многопериодных формул (`with_growth`, `with_cagr`,
moving average) `core/provenance.py` честно не мог указать точный
вклад(ы) периода и прикладывал провенанс по каждому периоду входа с
явной оговоркой (`note`) — этот предел был явно задокументирован ещё в
Фазе 10.

Теперь у `ComparisonTable` новое поле `cell_dependencies` —
`(period, column_key) -> ((input_column_key, input_period), ...)`,
записывается КАЖДОЙ функцией `with_*` в момент вычисления конкретной
ячейки, а не восстанавливается потом из текста формулы. Например,
`with_growth` для AFG в 2020 году пишет `[("AFG", "2019"), ("AFG",
"2020")]` — оба периода, участвовавших именно в этом расчёте; `with_cagr`
для диапазона 2015–2025 — ровно `[("AZE", "2015"), ("AZE", "2025")]`, без
промежуточных лет; `with_moving_average(window=3)` — все 3 периода
конкретного окна; `with_rank` — только те колонки, у которых было
значение именно в этом периоде (не все базовые колонки таблицы, если
у части нет данных на этот период). Обновлены все функции `compose.py`,
которые производят значения, включая новые из Фазы D
(`with_share_pair`/`with_per_capita_pair`/`with_index_column`).

`core/provenance.py::resolve_provenance()` теперь сначала смотрит в
`cell_dependencies` — если запись есть, идёт ровно по этим парам
(период, вход), точно, без оговорок, включая многопериодные формулы.
Старое поведение (по каждому периоду входа + `note`) осталось как честный
fallback — для гипотетической производной колонки, построенной не через
`with_*` (например, вручную в тесте) и потому без записи в
`cell_dependencies`; не потому что этот путь ожидаемо сработает для
чего-либо, что производит сам код проекта.

`cell_dependencies` сознательно не попадает в `ComparisonTable.as_dict()`
(JSON таблицы) — это внутренний механизм резолвера, а не то, что нужно
фронтенду напрямую; уже резолвленный provenance для запрошенной ячейки
приходит в `AskResult.provenance`, с тем же форматом, что и раньше.

**257 офлайн-тестов** (было 252).

**Фаза F готова**: `unit` и структурная статистическая семантика.

Раньше `SDMXProvider._to_series_result()` вообще не заполнял `SeriesResult.
unit`, даже когда discovery (Фаза 2) уже нашёл его для конкретного
индикатора (World Bank) — известный пробел, честно зафиксированный ещё в
матрице верификации (Фаза A). Два независимых источника unit теперь оба
задействованы, по принципу "не гадать по названию, а использовать то, что
реально известно":

1. **Структурно известный на уровне датафлоу** — `SDMXSourceConfig` получил
   `unit_label`/`semantics`; у Eurostat `NAMA_10_GDP` unit ЗАФИКСИРОВАН как
   ключевой параметр запроса (`CP_MEUR` — собственный документированный код
   Eurostat "Current prices, million euro"), поэтому это факт о всём
   датафлоу, а не догадка по имени индикатора — заполняется на каждый
   фетч и на каждую discovered-запись (`entries_from_dsd()`).
2. **Известный только per-индикатор, из каталога** — у World Bank/IMF unit
   не фиксирован на уровне датафлоу, но discovery (Фаза 2) уже кладёт его
   в `IndicatorEntry.unit`. `core/ask.py::_fetch_table()` теперь предпочитает
   `series.unit`, а при его отсутствии берёт `candidate.unit` (то, что уже
   вернул `engine.search_indicator()`) — тот же принцип "provider, если
   знает; иначе каталог", не изобретённое значение.

Новая структурная модель `StatisticalSemantics` (`core/models.py`):
`price_basis` (nominal/real/index/percent/percentage_points), `currency`,
`currency_scale`, `per_capita`, `seasonally_adjusted` — каждое поле `None`
("неизвестно") по умолчанию, заполняется только там, где источник
действительно это гарантирует (сейчас — Eurostat NAMA_10_GDP: `price_basis
="nominal"`, `currency="EUR"`, `currency_scale="millions"`). Проведено
через весь путь данных: `SDMXSourceConfig` → `SeriesResult`/`IndicatorEntry`
→ `IndicatorMeta`/`CandidateIndicator` → `ComparisonColumn` — тот же
сквозной паттерн, что уже был у `unit`/`frequency`/`geographic_coverage`.

Использовано, а не просто сохранено: `core/validation.py` получил
`price_basis_consistency` — предупреждение, когда у двух БАЗОВЫХ колонок
оба price_basis ИЗВЕСТНЫ и РАЗЛИЧАЮТСЯ (например, номинальный ряд рядом с
реальным без явного запроса на такое сравнение), тем же принципом
"неизвестное не считается противоречием", что уже применялся для
unit/frequency. `core/selection.py::score_candidate()` даёт небольшой бонус
кандидату с документированной семантикой — тот же паттерн, что уже был у
"unit is documented".

`Catalog`-схема пополнилась колонкой `semantics` — `CREATE TABLE IF NOT
EXISTS` не трогает уже существующий файл, поэтому добавлена явная,
идемпотентная миграция (`Catalog._migrate()`, `ALTER TABLE ... ADD COLUMN`
только если колонки ещё нет) — без неё уже существующий персистентный
`catalog.db` из Фазы B сломался бы на первой же записи после обновления;
проверено отдельным тестом, симулирующим файл со старой схемой.

**266 офлайн-тестов** (было 257).

**Фаза G готова**: реальный сквозной прогон `AnthropicPlanner` с настоящим
ключом Anthropic API. Полный отчёт —
[`docs/benchmarks/phase-g-live-planner-report.md`](./docs/benchmarks/phase-g-live-planner-report.md).
Коротко: API Anthropic реально доступен из этой песочницы (в отличие от
статистических API — `anthropic.com` разрешён сетевой политикой), поэтому
это первый раз в проекте, когда планировщик проверен вживую, не
`ScriptedPlanner`. Официальные API по-прежнему заблокированы, так что
ретрив проверен против синтетического, честно помеченного набора данных
(как в бенчмарках Фазы 13), а не настоящего World Bank.

Первый прогон всех 10 примерных вопросов задания дал **0 из 10** рабочих
таблиц — не из-за галлюцинаций модели, а из-за двух настоящих,
структурных пробелов: (1) модель писала имя страны ("Azerbaijan"), а не
код, который нужен провайдеру ("AZE") — системный промпт сам разрешал обе
формы, но нигде в пайплайне не было шага, который бы имя резолвил; (2) для
"...до последнего доступного года" модель иногда писала буквально
`"latest"` вместо `null`, что ломало уже существующую идиому "resolve to
latest" (она проверяет на `falsy`, а строка `"latest"` truthy).

Оба исправлены в коде, а не промпт-инжинирингом под конкретные 10
вопросов: новый `core/geography.py::resolve_geography()` (через
`pycountry` — тот же принцип "не писать свой X", что уже применялся к
`sdmx1`/`pxwebpy`) резолвит имя страны в канонический alpha-3 код;
`provider_ref_area()` конвертирует его в формат, который нужен конкретному
источнику (World Bank/IMF — alpha-3, Eurostat — alpha-2, по собственным
проверенным примерам `registry.py`). `build_query_plan()` теперь резолвит
географию (и `weighted_average.inputs`) один раз, до отбора/ретрива.
`_clean_period()` трактует `"latest"/"present"/"now"/"current"/"today"`
как `None` при разборе периодов из ЛЮБОГО планировщика — защищает и
`/plan`/`/ask` как API-эндпоинты, не только этот один промпт.

После исправления — **4 из 10** дают полную таблицу с проверенными
числами (не просто "не упало"): население Азербайджана, сравнение GDP per
capita по трём странам, индексирование GDP к 2015=100 (сверено с исходным
рядом — 134.27, математика верна), последний доступный GDP per capita.
Остальные 6 — честно НЕ баги ретрива: один вопрос ("какие страны
лидируют по безработице") — модель корректно оставила географию пустой
для открытого вопроса вместо того, чтобы выдумать список стран (система
работает как задумано — честно говорит "география не определена", а не
галлюцинирует топ-N); пять — промахи поиска по каталогу против нарочно
тонкого синтетического каталога этого прогона ("nominal GDP" не находит
запись "GDP (current US$)") — это территория Фазы C, той же самой,
отложенной пользователем до реального наполнения каталога, не другая.

Отдельно зафиксировано, не исправлено: для вопроса про индексирование
голый концепт "GDP" совпал с тремя кандидатами (номинальный/реальный/на
душу населения); детерминированный tie-break (по алфавиту indicator_id)
выбрал номинальный — разумный результат, но по алфавитной случайности, не
по осознанной политике "по умолчанию номинальный для голого 'GDP'".
Задокументировано как честный, не критичный design gap для будущей фазы.

**279 офлайн-тестов** (было 266).

## Реальная живая проверка (продолжение Фаз A/H)

Пользователь переключил окружение сессии на неограниченный сетевой доступ
— впервые в проекте `pytest -m network` и прямые запросы к живым API
реально выполнены, не спроектированы офлайн. Полный отчёт —
[`docs/architecture/provider-verification-matrix.md`](./docs/architecture/provider-verification-matrix.md)
(полностью переписан по реальным результатам).

**Найден и исправлен критический баг**, существовавший в проекте с самого
начала: `SDMXProvider._to_series_result()` (общий код для World Bank, IMF,
Eurostat) брал период из `index_tuple[-1]` — последнего элемента индекса
`sdmx.to_pandas()`, предполагая, что `TIME_PERIOD` всегда идёт последним.
Проверено вживую против всех трёх источников — это НИКОГДА не так:
`TIME_PERIOD` всегда идёт ПЕРВЫМ (`[TIME_PERIOD, REF_AREA, SERIES, FREQ]`
у World Bank, ещё длиннее у IMF). Значит, каждое единственное наблюдение,
когда-либо полученное через `SDMXProvider`, имело НЕВЕРНЫЙ период —
что-то вроде `"A"` или `"PUB"` вместо настоящего года. Офлайн-фикстура
(`tests/conftest.py`) декларировала измерения в том же неверном порядке,
поэтому баг был "согласован" с тестом и не ловился — оба были неправы
одинаково. Исправлено: период теперь ищется по ИМЕНИ измерения
(`"TIME_PERIOD"`), а не по позиции — корректно независимо от количества/
порядка измерений у любого источника. Офлайн-фикстура исправлена под
реально проверенный порядок, добавлен отдельный регрессионный тест с
`TIME_PERIOD` намеренно в середине. Проверено вживую после исправления:
`NY_GDP_MKTP_CD`/`AZE` — 2020: 42.69B, 2021: 54.83B, 2022: 78.81B, 2023:
72.43B — настоящие периоды, правдоподобные значения (скачок 2022 года
соответствует реальному всплеску цен на нефть/газ).

Дополнительные живые находки:
- **IMF discovery**: живой `DSD_CPI` не кладёт codelist ни на один
  `local_representation` измерения (что предполагал код, скопировав
  уже проверенный паттерн Eurostat) — реальные codelist'ы (343 страны, 15
  категорий COICOP) лежат на `concept_identity.core_representation`
  каждого измерения, уже инлайново в том же ответе. Исправлено с
  fallback-проверкой в `_enumerated_codelist()`.
- **IMF ref_area**: собственная тест-фикстура `sdmx1` использует
  устаревший числовой код `"111"` — проверено вживую, что реальный
  `CL_COUNTRY` его больше не содержит, только ISO alpha-3 (`"USA"`,
  `"AFG"`...). `"USA"` вживую возвращает 100+ настоящих месячных значений
  CPI. Хорошая новость: это ТОЧНО совпадает с уже готовой Фазой G
  (`resolve_geography()` уже производит alpha-3) — фикс нужен был только
  в устаревшем тестовом значении, не в архитектуре.
- **US Census**: живой запрос на `/data/{year}/{dataset}` без ключа
  реально возвращает 302 редирект на HTML-страницу "missing key"
  (`X-DataWebAPI-KeyError: 1`), не JSON — раньше это проявлялось
  непонятным `JSONDecodeError`. `variables.json` (discovery) ключа НЕ
  требует — тоже проверено вживую, настоящая асимметрия. Исправлено:
  `CensusProvider` читает `CENSUS_API_KEY` из окружения (тот же принцип,
  что `ANTHROPIC_API_KEY`) и кидает понятную `CensusMissingApiKeyError`,
  если ключа нет — вместо крипто-ошибки. Ключа в этой сессии нет, так что
  retrieval с ключом остаётся непроверенным вживую (сам механизм — да).
- **World Bank discovery в полном масштабе**: 1498 реальных индикаторов,
  ни одного с оставшейся точкой в ID — фикс Фазы A подтверждён не только
  юнит-тестом, но и на полном реальном каталоге.

Полный набор `pytest -m network`: было 7 из 10 (3 реальных падения), стало
**9 из 10** (1 честно пропущен — Census без ключа). Все живые тесты и
фиксы — в этом же раунде коммитов.

## Фаза C: качество поиска по каталогу

Полный отчёт —
[`docs/benchmarks/phase-c-search-quality-report.md`](./docs/benchmarks/phase-c-search-quality-report.md).
Собран бенчмарк из **54 концептов** (GDP/население/инфляция/безработица/
торговля/госфинансы/демография/социалка/энергетика — список задания плюс
запас сверх "50+"), каждый код в котором проверен вживую через
`catalog.get(source_id, indicator_id)` против реального каталога до
попадания в список — не по памяти. Это сразу поймало реальное расхождение:
`EN_ATM_CO2E_PC` (код выбросов CO2 на душу населения, ожидаемый по
обучающим данным) в живом каталоге не существует — World Bank заменил его
на `EN_GHG_CO2_PC_CE_AR5` по новой методологии GHG/AR5.

Живое исследование каталога вскрыло конкретные, воспроизводимые провалы
обычного bm25-ранжирования FTS5: запрос "GDP" ставил на первое место
`Personal remittances, received (% of GDP)` вместо флагманского
`GDP (current US$)` (тот вообще не попадал в топ-5); "population" не
показывал `Population, total` в топ-5 вообще, отдавая предпочтение узким
возрастным разбивкам просто потому что слово "population" в их названии
встречается чаще; "inflation" правильно ставил `FP_CPI_TOTL_ZG` на первое
место, но затем заполнял позиции 2-5 нерелевантными записями US Census,
чьё название содержит "inflation-adjusted" как сноску о методологии
дохода, а не индикатор инфляции. Отдельно нашли, что у `NY_GDP_MKTP_CD`-
не связанной записи `Short-term debt (% of total reserves)` совпадение с
"GDP" происходило только потому, что её `source_organization` — это
длинная цитата происхождения данных ("World Bank (WB), type: **GDP
estimates**"), а не короткое имя организации.

Измерен честный baseline (обычный bm25, `ORDER BY rank`, без изменений в
коде): **Top-1 37.0% (20/54), Top-3 51.9% (28/54), Top-5 61.1% (33/54)**.

Исправлено без эмбеддингов (явное требование этапа — сначала
оптимизировать и измерить лексическое/метаданное ранжирование), двумя
изменениями в `Catalog.search()`:

1. **Явные веса колонок bm25** (`name=10.0, keywords=2.0, unit=1.0,
   source_organization=0.0, geo=0.2`) вместо равного веса 1.0 у всех
   индексированных колонок по умолчанию — `source_organization` как
   источник релевантности практически обнулён (участвует в самом `MATCH`,
   но не влияет на ранжирование), поскольку живое исследование показало
   его вред как сигнала.
2. **Композитное пере-ранжирование** (`_composite_score()`, новая функция
   в `core/catalog.py`) поверх bm25-упорядоченного пула кандидатов
   (`max(limit*20, 200)`, не весь каталог — широкий префиксный запрос
   вроде `population*` даёт 2000+ совпадений): считает долю токенов
   запроса, покрытых названием (по префиксу и точным словом), и штрафует
   каждое непокрытое слово названия — со скидкой для небольшого списка
   типовых административных квалификаторов официальной статистики
   (`total`, `annual`, `modeled`, `national`, `estimate`...), чтобы
   `"Population, total"` не штрафовался так же, как реально сужающее слово
   вроде `"rural"`/`"youth"`/`"female"`.

Результат на тех же 54 концептах, тот же живой каталог: **Top-1 55.6%
(30/54), Top-3 68.5% (37/54), Top-5 77.8% (42/54)**. Все четыре конкретно
диагностированных провала устранены: "GDP" и "population" теперь ставят
флагманский индикатор на первое место, шум Census вытеснен из топ-3
"inflation", ложное совпадение по цитате (`Short-term debt ...`) уходит на
последнее место вместо конкуренции за топ. Честно задокументированы
оставшиеся пробелы (без исправления в этом раунде): отсутствие стемминга у
FTS5-токенизатора (`unicode61` без `porter` — "users" не матчит "using",
"rate" не матчит "ratio"; исправление требует пересоздания FTS5-таблицы на
уже населённом каталоге, отдельный раунд) и реальная межисточниковая
неоднозначность ("government expenditure" совпадает и с индикатором нацсчетов, и с классификационной записью IMF CPI — лексического сигнала
недостаточно, чтобы различить статистические домены).

Офлайн-регрессия (`tests/test_search_quality.py`, 5 тестов, без сети,
захардкоженная фикстура, воспроизводящая именно эти реальные провалы) +
воспроизводимый скрипт для живого каталога
(`scripts/benchmark_catalog_search.py`). **290 офлайн-тестов** (было 285).

## Фаза H: реальный сквозной бенчмарк на 100+ вопросов

Полный отчёт —
[`docs/benchmarks/phase-h-benchmark-report.md`](./docs/benchmarks/phase-h-benchmark-report.md).
110 вопросов в естественно-языковом стиле прогнаны через реальный
`answer_question()` против реально наполненного каталога и настоящего
живого retrieval (World Bank, Eurostat, US Census) — без фейковых
провайдеров, без синтетики.

Честная оговорка: `QuestionInterpretation` для каждого вопроса —
захардкоженная ground truth (`ScriptedPlanner`), не результат живого
вызова LLM — состояние shell в этой песочнице не сохраняется между
отдельными вызовами инструмента, поэтому вставленный ранее в чат
`ANTHROPIC_API_KEY` был жив только в рамках одного Bash-вызова. Точность
интерпретации (текст вопроса → структурный план) в этом раунде НЕ
измеряется — Фаза G уже проверила этот шаг живым `AnthropicPlanner`
(против синтетики). Эта фаза — наоборот: живой retrieval, скриптованная
интерпретация, измерение всего, что происходит ПОСЛЕ корректной
интерпретации: выбор индикатора, retrieval, расчёт трансформаций,
provenance, валидация.

Первый прогон (без пауз между запросами, без повторов) — **31.8%**
сквозного успеха (35/110): World Bank живой SDMX-эндпоинт не выдерживает
~100 последовательных запросов без пауз и отвечает стеной `502 Bad
Gateway`. Найдено и исправлено двумя слоями, каждый раз перепроверено
live:

1. **`SDMXProvider` вообще не имел retry** — временный сбой становился
   постоянным. Добавлен retry с backoff (3 попытки, 1с/2с) для 502/503/504
   и `ConnectionError`; настоящая ошибка (404) по-прежнему падает сразу.
   Подняло успех до **74.5%** (82/110).
2. Retry пережидает короткие сбои, но не устойчивое ограничение скорости —
   добавлена пауза между вопросами (0.5с) в самом бенчмарке. Подняло успех
   до **93.6%** (103/110).

Отдельно найден и исправлен настоящий баг выбора индикатора: запрос
"inflation" (USA) выбирал переменную дохода US Census вместо флагманского
CPI-индикатора WB. `core/selection.py::score_candidate()` оценивал
кандидатов только по метаданным каталога и полностью отбрасывал
собственное ранжирование `Catalog.search()` (Фаза C) — оба кандидата
содержали слово "inflation" в названии (один — как сам концепт, другой —
как несвязанный составной модификатор "inflation-adjusted"), у обоих не
было `geographic_coverage` для различения, счёт совпал точно, и решение
свелось к произвольной алфавитной сортировке `(source_id, indicator_id)`,
которая предпочла `US_CENSUS_ACS1` перед `WB_WDI`. Исправлено: позиция
кандидата в результатах `Catalog.search()` теперь передаётся дальше
(`CandidateIndicator.search_rank`) и учитывается в `score_candidate()`.

Итоговые метрики финального прогона: retrieval success 103/110 (93.6%),
Top-1 индикатор 95/105 (90.5%), корректность расчётов 10/10 (100%,
проверено пересчётом производной колонки из тех же живых сырых значений
таблицы — без внешних "golden" чисел), provenance присутствует у 101/110
(91.8%), валидация PASS/WARNING/FAIL/NONE — 87/16/0/7, сквозной успех
**103/110 (93.6%)**.

Ручная проверка по требованию задания: 5 значений, полученных через
реальный пайплайн, независимо перепроверены прямым запросом к
классическому REST API World Bank (`api.worldbank.org/v2/...` — код,
никак не связанный с `sdmx1`-провайдером проекта) — все 5 совпали
побитово (ВВП США, население Германии, инфляция Франции, безработица
Японии, ожидаемая продолжительность жизни в Британии за 2022 год).

Оставшиеся 7 честно задокументированных провалов: 1 — Eurostat реально не
покрывает госдолг Мексики (400 Bad Request, не баг проекта); 6 — те же
известные по Фазе C пробелы (отсутствие стемминга, межисточниковая
неоднозначность для "exports"/"imports"/"government expenditure"/
"industrial production"), теперь проявившиеся не только в ранжировании, но
и в реальном сбое retrieval (например, "industrial production" для
Британии/Польши выбрал категорию занятости US Census "Industrial
production managers" — количество людей с такой профессией, а не
индустриальный индекс). **295 офлайн-тестов** (было 290).

## Фаза I: OECD пересмотрен с живым доступом

Полный отчёт —
[`docs/benchmarks/phase-i-oecd-report.md`](./docs/benchmarks/phase-i-oecd-report.md).
OECD оставался незарегистрированным с Фазы 5 (и весь раунд живой проверки)
— не было ни одного проверенного рабочего примера, а единственный реально
подтверждённый фильтрованный запрос требовал legacy-эндпоинт
`stats.oecd.org` с небезопасным понижением TLS-рукопожатия (сами
мейнтейнеры `sdmx1` документируют это как отключение защиты от
атак "человек посередине" — компромисс, на который проект не шёл).

С живым доступом выяснилось: `sdmx.Client("OECD")` в `sdmx1` теперь сам
указывает на `https://sdmx.oecd.org/public/rest` — ТЕКУЩИЙ официальный
эндпоинт OECD, не устаревший `stats.oecd.org`. `sdmx.Client("OECD").
dataflow()` живьём вернул 1544 реальных датафлоу без всякого TLS-обхода.

DSD национальных счетов (`DSD_NAMAIN10`) имеет 12 недатовых измерений
(против 3 у World Bank, 4 у Eurostat) — подбор правдоподобных значений из
кодлистов не сработал (400/404 на комбинациях, которые выглядели разумно).
Рабочий подход: живой запрос с полностью открытыми (wildcard) измерениями
кроме REF_AREA для одной страны, чтение реальных значений измерений из
настоящей вернувшейся строки для `TRANSACTION=B1GQ` (код ВВП). Итоговый
проверенный ключ `A.USA.S1.S1.B1GQ._Z._Z._Z.USD_EXC.V.N.T0102` сверен с
собственным значением World Bank для ВВП США за 2022 год — точное
совпадение (26 054 614 млн долл.), проверено тремя независимыми путями
(прямой curl к `sdmx.oecd.org`, вызов `sdmx1`, вызов через собственный
`SDMXProvider`).

Discovery оказался даже проще, чем у Eurostat (без промежуточного
external-reference резолва, как у IMF) — переиспользован общий
`sdmx_discovery.py::entries_from_dsd()`, новая архитектура не потребовалась.
Зарегистрирован один датафлоу `OECD_NAMAIN10` (`{indicator}` → измерение
TRANSACTION, остальные 9 недатовых измерений закреплены проверенными
значениями). Реальный `ustat catalog refresh OECD_NAMAIN10` обнаружил и
внёс **308 настоящих индикаторов**.

Честно: флагманский индикатор (ВВП, `B1GQ`) надёжен — проверен вживую для
7 стран (США, Германия, Франция, Япония, Британия, Италия, Южная Корея),
все значения реальные и правдоподобные. НЕ каждый из 308 обнаруженных
кодов TRANSACTION гарантированно резолвится под этим же зафиксированным
ключом — два других кода (`P3`, `P51G`) для других стран вернули `404
NoResultsFound`, тот же класс проблемы "заявлено в кодлисте, но не
заполнено для этой комбинации", что уже задокументирован в Фазе H для
Eurostat/Census. Матрица провайдеров обновлена честно: OECD теперь
PARTIALLY VERIFIED (не полный VERIFIED) — ВВП-запросы надёжны, остальные
концепты из этого источника требуют проверки по коду.

**302 офлайн-теста** (было 295) + 7 новых офлайн-тестов на
`OECDProvider` + 2 живых сетевых теста, оба реально прогнаны и прошли.
